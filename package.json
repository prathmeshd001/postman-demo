{
  "name": "postman-newman-demo",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test:api": "npx newman run Demo.postman_collection.json -e demoenv.postman_environment.json --env-var baseUrl=http://localhost:3000 -r cli,htmlextra --reporter-htmlextra-export reports/index.html --reporter-htmlextra-title \"API Test Report\"",
    "ci": "node server.js & npx wait-on http://localhost:3000 && curl -s -X POST http://localhost:3000/__reset || true && npm run test:api"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "newman": "^6.2.1",
    "newman-reporter-htmlextra": "^1.23.1",
    "wait-on": "^7.2.0"
  }
}
